services:
  - type: web
    name: sota-official
    env: static
    buildCommand: npm install && npm run build -- --configuration=production
    staticPublishPath: dist/test-frontend
    routes:
      - type: rewrite
        source: /.*
        destination: /index.html
